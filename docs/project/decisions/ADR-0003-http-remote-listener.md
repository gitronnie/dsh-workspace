# ADR-0003：允许 HTTP 的独立 listener

- 状态：accepted
- 日期：2026-08-14

## 决策

插件独立监听默认端口 `3090`，首次安装只绑定回环地址。本机显式启用后重绑定管理员配置的监听地址；初始远程地址为所有 IPv4 接口。HTTP 和 HTTPS 反向代理都受支持，不强制证书。监听地址与端口的后续可配置决策见 ADR-0004。

## 原因

DSH 主 Web listener 的绑定策略和私有协议不应成为移动客户端契约。允许 HTTP 降低局域网使用门槛；配对、令牌、scope、限流、审计和醒目风险说明仍然是强制控制。
