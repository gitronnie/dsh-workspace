# ADR-0002：授权根与相对路径

- 状态：accepted
- 日期：2026-08-14

## 决策

管理员在回环管理面登记真实根路径，外部 API 只接受不透明 `rootId` 和 POSIX 风格相对路径。设备需要 scope 与 root grant 两层授权。

## 原因

该模型允许用户选择任意本机目录，同时避免外部调用者提交服务器绝对路径。逐段链接检查把目录逃逸控制集中在一个跨平台边界中。
